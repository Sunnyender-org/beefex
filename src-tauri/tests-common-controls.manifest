<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<!--
  cargo test 构建的测试二进制不经 tauri_build 的资源/清单嵌入。项目依赖（rfd/tauri-plugin-dialog）
  静态导入 comctl32.dll 的 TaskDialogIndirect —— 该导出仅存在于 Common-Controls v6（WinSxS，靠应用
  清单激活），System32 默认的 v5.82 没有。缺清单时测试 exe 加载即 STATUS_ENTRYPOINT_NOT_FOUND
  (0xC0000139)，cargo test 一条都跑不了。

  不能在 build.rs 里用链接参数嵌入：唯一覆盖 lib 单测 harness 的 rustc-link-arg 是全目标级，会同时
  作用于主程序 bin，与 tauri 已嵌入的资源清单撞车（CVT1100 资源重复 / LNK1123）。Windows 也会
  优先使用 Rust test harness 已带的内嵌清单，旁置同名外部清单不会补上 v6 依赖。

  scripts/win-cargo-test.ps1 因此先构建 test harness，再用 Windows SDK mt.exe 只给 beefex-* 测试
  二进制合并本清单。应用主程序仍由 Tauri 自己管理资源和清单。
-->
<assembly xmlns="urn:schemas-microsoft-com:asm.v1" manifestVersion="1.0">
  <dependency>
    <dependentAssembly>
      <assemblyIdentity
        type="win32"
        name="Microsoft.Windows.Common-Controls"
        version="6.0.0.0"
        processorArchitecture="*"
        publicKeyToken="6595b64144ccf1df"
        language="*"/>
    </dependentAssembly>
  </dependency>
</assembly>
